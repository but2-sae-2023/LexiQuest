<!-- <?php
// // Chemin vers le répertoire du programme C
// $cProgramPath = "../LexiQuest-C";

// // Commande pour compiler le programme C (build_lex_index)
// $compileBuildIndexCommand = "cd $cProgramPath && gcc ./build_lex_index";
// exec($compileBuildIndexCommand, $compileBuildIndexOutput, $compileBuildIndexResultCode);

// // Affichage des résultats de la compilation pour build_lex_index
// echo "Résultat de la compilation pour build_lex_index : $compileBuildIndexResultCode\n";
// echo "Sortie de la compilation pour build_lex_index :\n";
// echo implode("\n", $compileBuildIndexOutput) . "\n\n";

// // Commande pour compiler le programme C (new_game)
// $compileNewGameCommand = "cd $cProgramPath && gcc ./new_game";
// $compileNewGameOutput = shell_exec($compileNewGameCommand);
// $compileNewGameResultCode = ($compileNewGameOutput !== null) ? 0 : 1;

// // Affichage des résultats de la compilation pour new_game
// echo "Résultat de la compilation pour new_game : $compileNewGameResultCode\n";
// echo "Sortie de la compilation pour new_game :\n";
// echo $compileNewGameOutput . "\n\n";

// // Commande pour compiler le programme C (add_word)
// $compileAddWordCommand = "cd $cProgramPath && gcc ./add_word";
// $compileAddWordOutput = shell_exec($compileAddWordCommand);
// $compileAddWordResultCode = ($compileAddWordOutput !== null) ? 0 : 1;

// // Affichage des résultats de la compilation pour add_word
// echo "Résultat de la compilation pour add_word : $compileAddWordResultCode\n";
// echo "Sortie de la compilation pour add_word :\n";
// echo $compileAddWordOutput . "\n\n";

// // Commande pour exécuter le programme C (a.out)
// $executeCommand = "$cProgramPath/./a.out";

// // Exécution de la commande d'exécution
// $executeOutput = shell_exec($executeCommand);
// $executeResultCode = ($executeOutput !== null) ? 0 : 1;

// // Affichage des résultats de l'exécution
// echo "Résultat de l'exécution : $executeResultCode\n";
// echo "Sortie de l'exécution :\n";
// echo $executeOutput . "\n";

echo "test";
?> -->