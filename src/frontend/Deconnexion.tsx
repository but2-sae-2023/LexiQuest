import '../style/App.css';


const StartButton = () => {
    //useContext(UserContext);

    return (
        <>
            <button>Se déconnecter</button>
        </>
    );
};

export default StartButton;